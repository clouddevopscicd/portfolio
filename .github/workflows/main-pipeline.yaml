name: CICD pipeline with SonarCloud, Docker, Trivy, Deploy

on:
  push:
    branches: [ main, release ]
  pull_request:
    branches: [ main, release ]
  workflow_dispatch:

env:
  IMAGE_NAME: ${{ github.repository }}
  REGISTRY: docker.io/${{ vars.DOCKER_USERNAME }}

jobs:

  build-and-test:
    name: Build, Lint, Test with Matrix
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: [ 3.11, 3.12 ]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug Python Version
        run: echo "Python version is ${{ matrix.python-version }}"

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Dependencies
        run: |
          set -e
          pip install -r requirements.txt
          pip install flake8 pytest

      - name: Set PYTHONPATH
        run: echo "PYTHONPATH=$PWD" >> $GITHUB_ENV

      - name: Lint Application
        run: |
          set -e
          flake8 .

      - name: Run Unit Tests
        run: |
          set -e
          pytest tests

  sonarcloud-analysis:
    name: SonarCloud Static Analysis
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v5.0.0
        with:
          projectBaseDir: .
          args: >
            -Dsonar.organization=clouddevopscicd
            -Dsonar.projectKey=clouddevopscicd_portfolio
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_GITHUB_USERTOKEN }}

  docker-build-scan:
    name: Build Docker Image, Scan with Trivy, Push to Registry
    runs-on: self-hosted
    needs: sonarcloud-analysis

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify Docker & Trivy Setup
        run: |
          set -e
          docker --version || { echo "🚨 Docker not installed or not running"; exit 1; }
          trivy --version || { echo "🚨 Trivy not installed or not working"; exit 1; }
          echo "✅ Docker and Trivy setup confirmed"

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Docker Login
        uses: docker/login-action@v2
        with:
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image with Logs
        run: |
          set -e
          echo "🚀 Building Docker Image: $REGISTRY/$IMAGE_NAME:${{ github.sha }}"
          docker build -t $REGISTRY/$IMAGE_NAME:${{ github.sha }} .
          echo "✅ Docker Image build complete"

      - name: Run Trivy Vulnerability Scan with Logs
        run: |
          set -e
          echo "🔍 Starting Trivy Scan on $REGISTRY/$IMAGE_NAME:${{ github.sha }}"
          trivy image --severity CRITICAL,HIGH --exit-code 1 -f json -o trivy-report.json $REGISTRY/$IMAGE_NAME:${{ github.sha }}
          echo "✅ Trivy Scan completed, report saved"

      - name: Upload Trivy Report (Always Runs)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-report
          path: trivy-report.json

      - name: Push Docker Image with Logs
        run: |
          set -e
          echo "📦 Pushing Docker Image to $REGISTRY/$IMAGE_NAME:${{ github.sha }}"
          docker push $REGISTRY/$IMAGE_NAME:${{ github.sha }}
          echo "✅ Docker Image pushed successfully"

  deploy-to-vm:
    name: Deploy Docker Image on RedHat VM
    runs-on: self-hosted
    needs: docker-build-scan

    steps:
      - name: Docker Login
        run: |
          set -e
          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}

      - name: Pull & Run Container with Logs
        run: |
          set -e
          echo "📥 Pulling Docker Image: $REGISTRY/$IMAGE_NAME:${{ github.sha }}"
          docker pull $REGISTRY/$IMAGE_NAME:${{ github.sha }}
          echo "🛑 Stopping and removing old container if exists"
          docker stop app || true
          docker rm app || true
          echo "🚀 Running new container on port 8000"
          docker run -d -p 8000:8000 --name app $REGISTRY/$IMAGE_NAME:${{ github.sha }}
          echo "✅ Application container is running"
